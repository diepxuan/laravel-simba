## 2026-02-23 - Command Registration Fixes và Service Cleanup

### 🔧 PR #77 - Fix Stored Procedure Parameters (ĐÃ MERGE)
- **Vấn đề**: Lỗi `SQLSTATE[42000]: Procedure or function asINGetDMKHO has too many arguments specified` trên trang `/khohang/tonkho`
- **Nguyên nhân**: 
  - Stored procedure gốc chỉ có 3 tham số (`@pMa_cty`, `@pMa_kho`, `@pStruct`)
  - Ứng dụng đang gọi với 4 tham số (thừa `@pLanguage`)
  - Class `AsINGetDMKHO` định nghĩa với 0 tham số
- **Giải pháp**:
  1. Sửa class `AsINGetDMKHO.php` chỉ định nghĩa 3 tham số, bỏ `pLanguage`
  2. Update model `InDmKho.php` gọi `AsINGetDMKHO::call()` thay vì `DB::select('EXEC...')`
  3. Cập nhật các stored procedure calls khác trong cùng model để dùng class
- **Status**: ✅ Đã merge

### 🔧 PR #78 - Prevent Command Registration Issues (ĐANG CHỜ REVIEW)
- **Vấn đề**: Command registration không hoạt động đúng cho tất cả packages
- **Nguyên nhân**:
  1. **Inconsistent config file naming**: Tất cả packages có `config.php` nhưng `Package::getCommands()` expect config key matching package name
  2. **Manual command registration**: Một số ServiceProviders có manual `$this->commands()` calls
  3. **Namespace issues**: Config files dùng `Class::class` constants
- **Giải pháp**:
  1. **Renamed all config.php files** → `{package-name}.php` (10 packages)
  2. **Fixed namespace definitions**: `Class::class` → `'Namespace\\Class'` (string format)
  3. **Cleaned up ServiceProviders**: Xóa manual command registration
  4. **Removed redundant portal-fixed.service**: File cũ dư thừa, dùng command `serve:dev:service` thay thế
- **Benefits**:
  - `Package::getCommands()` hoạt động đúng cho tất cả packages
  - Consistent config system
  - No duplicate command registration
  - Auto-discovery works correctly
- **Status**: ⏳ Đang chờ review

### 📚 Bài học về Package System
1. **Config file naming**: Phải match package name để `config('{package}.namespace')` hoạt động
2. **Namespace strings**: Dùng string thay vì `Class::class` để tránh Laravel load class trước khi config ready
3. **Auto-registration**: Core ServiceProvider auto-register commands qua `Package::getCommands()`
4. **Không manual registration**: Tránh `$this->commands()` trong ServiceProviders để không trùng lặp

### 🗑️ File đã xóa
- `portal-fixed.service`: File service cũ dư thừa, thay bằng command `php artisan serve:dev:service install`

### 🔄 Workflow đã áp dụng
1. Tạo branch mới từ main sau khi PR #77 merge
2. Fix issues phát hiện trong code review
3. Tạo PR tập trung vào specific problem
4. Chờ review và merge

### 📝 Notes cho tương lai
- Kiểm tra tất cả packages khi có changes đến config/command system
- Đảm bảo consistency trong naming conventions
- Ưu tiên dùng auto-discovery thay vì manual registration

### 🔧 CRITICAL FIX: ConfigServiceProvider Update (23/02/2026)
- **Vấn đề phát hiện**: Sau khi đổi tên `config.php` → `{package}.php`, `ConfigServiceProvider` vẫn tìm file `config/config.php` - sẽ phá hủy toàn bộ core system!
- **Giải pháp**: Sửa `ConfigServiceProvider.php` để:
  1. Ưu tiên tìm `config/{code}.php` (new convention)
  2. Fallback tìm `config/config.php` (backward compatibility)
  3. Đảm bảo `config('{code}.namespace')` hoạt động
- **Tại sao quan trọng**: Nếu không fix, `Package::getCommands()` sẽ không hoạt động → không có commands nào được auto-registered → toàn bộ artisan commands system broken

### 📚 Documentation Created: CORE_PACKAGE_LOADING_DOCS.md
- Viết comprehensive documentation về core package loading system
- Giải thích cách `Package::list()`, `Package::getCommands()` hoạt động
- Các quy tắc KHÔNG ĐƯỢC VI PHẠM:
  1. Config file naming: `{package-code}.php` hoặc `config.php`
  2. Config content: Dùng string, không dùng `Class::class`
  3. Config key: Phải match package code từ `Package::list()`
  4. Không manual command registration: Chỉ dùng auto-discovery
- Testing procedures và common errors/fixes
- Architecture overview và checklist trước khi thay đổi core

### 🚨 Bài học quan trọng:
1. **LUÔN KIỂM TRA BACKWARD COMPATIBILITY**: Khi thay đổi naming convention, phải update tất cả components phụ thuộc
2. **HIỂU RÕ CORE ARCHITECTURE**: Trước khi thay đổi, phải hiểu cách các components tương tác
3. **TEST COMPREHENSIVELY**: Test config loading và command registration sau mỗi change
4. **DOCUMENT CHANGES**: Viết documentation để các AI agent sau không mắc lỗi tương tự

### ✅ PR #78 Hiện bao gồm:
1. Standardize package config files
2. Fix command registration issues
3. Remove redundant portal-fixed.service
4. **CRITICAL**: Fix ConfigServiceProvider để support new config naming
5. **DOCS**: Viết comprehensive documentation về core system

### 🔄 Workflow cải tiến:
1. **Analyze dependencies**: Trước khi thay đổi, phân tích tất cả components phụ thuộc
2. **Update core components**: Đảm bảo core components hỗ trợ cả new và legacy conventions
3. **Test end-to-end**: Test từ config loading đến command registration
4. **Document changes**: Viết docs để maintain consistency trong tương lai

---

## 2026-02-23 - Markdown Conversion Completion (96.4% Coverage)

### 📊 Van-Ban Project: Markdown Conversion Status
- **Tổng đề mục**: 306 files HTML
- **Đã convert sang Markdown**: 295 files (96.4% coverage)
- **Còn lại**: 11 files (3.6%) - files quá lớn (>3.6MB mỗi file)

### ✅ Đã hoàn thành:
1. **Database setup**: 45 chủ đề, 306 đề mục, 76,303 điều khoản
2. **HTML content import**: 306/306 đề mục có nội dung HTML trong `de_muc_content`
3. **Markdown conversion**: 295/306 đề mục đã convert sang Markdown trong `de_muc_markdown`
4. **Markdown pages generation**: 342 markdown files đã được tạo

### 📈 Kết quả Conversion:
- **Coverage**: 96.4% (295/306)
- **Compression rate**: 20-80% kích thước giảm (HTML → Markdown)
- **Database tables**:
  - `de_muc_content`: 306 records (HTML gốc)
  - `de_muc_markdown`: 295 records (Markdown đã convert)

### ⚠️ Files lớn còn lại (chưa convert):
1. **Xử lý vi phạm hành chính** - 14.4MB
2. **Kế toán** - 8.4MB  
3. **Quản lý ngoại thương** - 4.0MB
4. **Quản lý, sử dụng tài sản công** - 4.0MB
5. **Chứng khoán** - 3.6MB

### 🔧 Issues gặp phải:
1. **MySQL max_allowed_packet**: Files >5MB gây lỗi "Got a packet bigger than 'max_allowed_packet' bytes"
2. **Connection timeout**: Mất kết nối MySQL server khi xử lý batch lớn
3. **Memory usage**: Files lớn tốn nhiều memory khi convert

### 🛠️ Solutions đã áp dụng:
1. **Batch processing**: Chia nhỏ thành batch 10-20 files
2. **Skip large files**: Tạm skip files >5MB
3. **Retry logic**: Tự động retry khi mất kết nối
4. **Simple conversion**: Dùng regex đơn giản thay vì parser phức tạp

### 📝 Documentation created:
1. **MARKDOWN_CONVERSION_SUMMARY.md**: Tổng kết kết quả và next steps
2. **Scripts**: `convert_with_retry.py`, `convert_skip_large_files.py`, `complete_markdown_conversion.py`

### 🎯 Next Steps (nếu cần):
1. **Tăng max_allowed_packet** trên MySQL server để xử lý files lớn
2. **Chunk processing**: Chia files lớn thành nhiều phần
3. **Compression**: Nén content trước khi insert
4. **Update generation scripts**: Sửa `generate_vanban.py` để dùng Markdown thay vì HTML

### 📚 Bài học:
1. **Kiểm tra kích thước files** trước khi xử lý batch lớn
2. **Xử lý files lớn riêng** với strategy khác
3. **Retry logic quan trọng** với database operations
4. **96.4% coverage có thể đủ** cho hầu hết use cases

### 🔄 Workflow cho data processing:
1. **Analyze data size**: Kiểm tra kích thước và distribution trước
2. **Handle edge cases**: Files quá lớn/quá nhỏ cần xử lý riêng
3. **Batch với retry**: Chia nhỏ và có retry logic
4. **Monitor progress**: Theo dõi coverage và errors real-time
5. **Document results**: Ghi lại kết quả và lessons learned

---

## 2026-02-23 - Script Consolidation và Markdown Regeneration

### 📋 PR #51 - Script Consolidation và Markdown Regeneration
- **PR Link**: https://github.com/diepxuan/diepxuan.github.io/pull/51
- **Branch**: `feat/vanban-database-docs` (cùng branch với PR #50)
- **Status**: ✅ Đã push tất cả changes, sẵn sàng review

### 🎯 Task hoàn thành: Review và merge scripts, remove redundant ones, regenerate markdown files
1. **Script consolidation**: 34 scripts → 1 script duy nhất (`vanban_generator.py`)
2. **Markdown regeneration**: Tạo lại tất cả markdown files từ database
3. **Cleanup**: Xoá files trùng lặp, sai, thiếu
4. **Documentation**: Tạo README đầy đủ cho scripts

### 🔧 Script Consolidation Results:
- **Scripts trước**: 34 files Python trong `scripts/`
- **Scripts sau**: 1 file duy nhất (`vanban_generator.py`)
- **Archive**: 33 scripts dư thừa đã chuyển vào `scripts/archive/`
- **Reduction**: 97% scripts được loại bỏ

### 📊 Markdown Regeneration Results:
- **Files cũ đã xoá**: 663 markdown files
- **Files mới tạo**: 352 markdown files từ database
- **Backward compatibility**: 293 files với dấu gạch dưới
- **Tổng files**: 645 markdown files (352 + 293)

### 🎯 Script duy nhất: `vanban_generator.py`
- **Chức năng**:
  1. Xoá tất cả markdown files cũ
  2. Kết nối database MySQL `vbpl`
  3. Lấy dữ liệu: 45 topics, 306 subjects, 76,303 provisions
  4. Tạo markdown files với cấu trúc URL đúng
  5. Tạo backward compatibility files
- **URL structure**:
  - Home: `/van-ban/`
  - Topic: `/van-ban/<topic-slug>/`
  - Subject: `/van-ban/<topic-slug>/<subject-slug>/`

### 📚 Documentation Created: `scripts/README.md`
- **Nội dung**: Hướng dẫn đầy đủ về scripts, database structure, workflow
- **Scripts dư thừa**: Danh sách 33 scripts đã archive
- **Quick start**: Hướng dẫn chạy script chính
- **Troubleshooting**: Các vấn đề thường gặp và giải pháp

### 🗑️ Files đã xoá/cleanup:
1. **UUID files**: Tất cả files với UUID trong tên
2. **Redundant markdown**: Files trùng lặp nội dung
3. **Incorrect structure**: Files sai cấu trúc
4. **Missing content**: Files thiếu nội dung

### 📈 Database Statistics (đã xác nhận):
- **Topics**: 45
- **Subjects**: 306  
- **Legal provisions**: 76,303
- **Content size**: 6.5MB across all provisions

### 🚀 Benefits:
1. **Single source of truth**: Chỉ 1 script để generate tất cả markdown
2. **Consistent URL structure**: Cấu trúc URL chuẩn, dễ navigate
3. **Full content coverage**: 100% provisions từ database
4. **Backward compatibility**: Vẫn hỗ trợ old URLs
5. **Clean codebase**: Không còn scripts dư thừa

### 🔄 Git Workflow Applied:
1. **Commit changes**: `git add . && git commit -m "feat: consolidate scripts and regenerate markdown files..."`
2. **Push to existing branch**: `git push origin feat/vanban-database-docs`
3. **PR auto-updated**: PR #51 được cập nhật tự động với tất cả changes

### 📝 Bài học về Script Management:
1. **Consolidation**: Nhiều scripts nhỏ → 1 script mạnh
2. **Documentation**: README quan trọng cho maintenance
3. **Archive strategy**: Giữ scripts cũ trong archive thay vì xoá hẳn
4. **Single responsibility**: 1 script làm 1 việc tốt hơn nhiều scripts làm nhiều việc nhỏ

### 🔧 Technical Implementation:
1. **Database connection**: MySQL connector với retry logic
2. **File operations**: Atomic operations với error handling
3. **Slug generation**: Tự động tạo slugs từ Vietnamese text
4. **Backward compatibility**: Tự động tạo files với dấu gạch dưới

### 🎯 PR #51 Ready for Review:
- **Changes**: 641 files modified (markdown files)
- **Additions**: 2 files (README.md, vanban_generator.py)
- **Deletions**: 34 files (scripts dư thừa)
- **Status**: ✅ Đã push thành công, sẵn sàng review và merge

### 🔄 Workflow cho Script Consolidation:
1. **Analyze existing scripts**: Hiểu chức năng của từng script
2. **Identify common patterns**: Tìm logic chung có thể hợp nhất
3. **Create unified script**: Viết script mới với tất cả chức năng
4. **Test thoroughly**: Test với real data
5. **Document**: Viết README và migration guide
6. **Archive old scripts**: Chuyển scripts cũ vào archive
7. **Update PR**: Commit và push tất cả changes